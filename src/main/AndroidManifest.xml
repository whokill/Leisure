<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.linghouse.leisure">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/leisurely"
        android:label="@string/app_name"
        android:name=".App.MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.RegisterActivity"/>
        <activity android:name=".UI.FinalRegisActivity"/>
        <activity android:name=".UI.ReleaseGoodsActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".UI.LoginActivity"/>
        <activity android:name=".UI.SearchActivity"/>
        <activity android:name=".UI.GoodDetailsActivity"/>
        <activity android:name=".UI.ShoppingAddressActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"/>
    </application>
</manifest>